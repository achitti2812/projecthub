{
  "version": 2,
  "buildCommand": "npx prisma generate && npx tsc",
  "installCommand": "npm install",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
